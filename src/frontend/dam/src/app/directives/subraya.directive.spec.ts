import { SubrayaDirective } from './subraya.directive';

describe('SubrayaDirective', () => {
  it('should create an instance', () => {
    const directive = new SubrayaDirective();
    expect(directive).toBeTruthy();
  });
});
